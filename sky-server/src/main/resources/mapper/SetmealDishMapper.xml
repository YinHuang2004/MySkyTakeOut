<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
<<<<<<< HEAD
    <select id="countRelateSetmeal" resultType="java.lang.Long">
        select count(*)
        from setmeal_dish
        where dish_id in <foreach collection="list" item="dishId" separator="," open="(" close=")">
        #{dishId}
    </foreach>
    </select>
    <insert id="insertBatch" parameterType="list">
        insert into setmeal_dish
        (setmeal_id,dish_id,name,price,copies)
        values
        <foreach collection="setmealDishes" item="sd" separator=",">
            (#{sd.setmealId},#{sd.dishId},#{sd.name},#{sd.price},#{sd.copies})
        </foreach>
    </insert>
    <delete id="deleteBySetmealIds">
        delete from setmeal_dish
        where setmeal_id in <foreach collection="list" item="setmealId" separator="," open="(" close=")">
        #{setmealId}
    </foreach>
    </delete>
    <select id="getBySetmealId" resultType="com.sky.entity.SetmealDish">
        select * from setmeal_dish where setmeal_id=#{id}
    </select>
=======
    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        select
            setmeal_id
        from setmeal_dish
            dish_id in
            <foreach collection="list" item="id" open="(" close=")" separator=",">
                #{id}
            </foreach>
    </select>
<!--    一个套餐有多个菜品记得要加循环-->
    <insert id="insertBatch">
        insert into setmeal_dish(setmeal_id,dish_id,name,price,copies)
        values
            <foreach collection="list" item="sd" separator=",">(#{sd.setmealId},#{sd.dishId},#{sd.name},#{sd.price},#{sd.copies})</foreach>


    </insert>
    <delete id="deleteBatch">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="list" item="setMealId" separator="," open="(" close=")">
            #{setMealId}
        </foreach>
    </delete>
>>>>>>> 424555f2080e30ea9bb7a8fb209e617ef52310b6
</mapper>