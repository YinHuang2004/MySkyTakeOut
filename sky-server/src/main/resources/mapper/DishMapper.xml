<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
<<<<<<< HEAD
    <select id="countByCategoryId" resultType="java.lang.Integer">
        select count(id) from dish where category_id = #{categoryId}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description, create_time, update_time, create_user,update_user, status)
        values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, #{status})
    </insert>
    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select d.* , c.name as categoryName from dish d left outer join category c on d.category_id = c.id
        <where>
            <if test="name != null">
                and d.name like concat('%',#{name},'%')
=======
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description, create_time, update_time, create_user,update_user, status)
        values (#{name}, #{categoryId}, #{price}, null, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, #{status})
    </insert>
    <select id="getTotal" resultType="java.lang.Long">
        select count(*) from dish d
        <where>
            <if test="name != null and name != ''">
                and d.name like concat('%', #{name}, '%')
>>>>>>> 424555f2080e30ea9bb7a8fb209e617ef52310b6
            </if>
            <if test="categoryId != null">
                and d.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and d.status = #{status}
            </if>
        </where>
<<<<<<< HEAD
        order by d.create_time desc
    </select>
    <select id="countOnSale" resultType="java.lang.Long">
        select count(*) from dish where status=1
        and id in <foreach collection="list" item="id" open="(" close=")" separator=",">#{id}</foreach>
    </select>
    <delete id="deleteBatch">
        delete from dish where id in <foreach collection="list" item="id" open="(" close=")" separator=",">#{id}</foreach>
    </delete>
    <select id="getById" resultType="com.sky.entity.Dish">
=======
    </select>
    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select
            d.id,d.name,d.category_id,d.price,d.image,d.description,d.status,d.update_time,c.name as categoryName
        from dish d
        left join category c
        on d.category_id=c.id
        <where>
            <if test="name!=null">d.name like concat('%',#{name},'%')</if>
            <if test="status!=null">and d.status =#{status}</if>
            <if test="categoryId!=null">and d.category_id=#{categoryId}</if>
        </where>
        order by d.create_time desc
        limit #{page},#{pageSize}
    </select>
    <delete id="deleteByIds">
        delete from dish where id in <foreach collection="list" item="id" open="(" close=")" separator="," >
        #{id}
    </foreach>
    </delete>
    <select id="countEnableDishByIds" resultType="java.lang.Long">
        select count(*)
        from dish
        where id in <foreach collection="list" item="id" separator="," open="(" close=")">
        #{id}
    </foreach>
    and status=1
    </select>
    <select id="getInfoById" resultType="com.sky.entity.Dish">
>>>>>>> 424555f2080e30ea9bb7a8fb209e617ef52310b6
        select * from dish where id=#{id}
    </select>
    <update id="update">
        update dish
        <set>
            <if test="name!=null">name=#{name},</if>
            <if test="categoryId!=null"> category_id=#{categoryId},</if>
            <if test="price!=null"> price=#{price},</if>
            <if test="image!=null"> image=#{image},</if>
<<<<<<< HEAD
            <if test="description"> description=#{description},</if>
=======
            <if test="description!=null"> description=#{description},</if>
>>>>>>> 424555f2080e30ea9bb7a8fb209e617ef52310b6
            <if test="status!=null">status=#{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id=#{id}
    </update>
<<<<<<< HEAD
    <select id="getDishByCategoryId" resultType="com.sky.entity.Dish">
        select * from dish where category_id = #{categoryId} and status=1
    </select>
</mapper>


=======
    <select id="getDishByCateId" resultType="com.sky.entity.Dish">
        select * from dish where category_id=#{categoryId}
    </select>
</mapper>
>>>>>>> 424555f2080e30ea9bb7a8fb209e617ef52310b6
